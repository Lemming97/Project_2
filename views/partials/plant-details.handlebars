<section class="card">
  <a href="/plant/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">
      <p>Likes to Bloom in: {{ bloom_time}}</p>
    </div>
  </a>
</section>
